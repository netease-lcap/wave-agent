# Wave 桌面版

Wave 桌面版是一个独立的 Electron 应用，无需安装 IDE 即可使用 Wave 代码智聊。它复用与 IDE 插件完全相同的聊天界面，通过 stdio 连接内置的 CLI 后端，并在其上提供分屏多对话、对话级面板、localhost 原型预览等桌面独有能力。

> 核心交互（对话、工具调用、权限确认、斜杠命令、配置等）与 IDE 插件完全一致，请参考 [VS Code 扩展 / JetBrains 插件](/vsce) 文档。本页只介绍桌面版独有的功能。

## 首次启动：选择工作目录

每次启动都是全新开始 —— 不记住上次的工作目录和会话。未选择工作目录时，输入区（含 +、斜杠、权限、发送按钮）整体禁用，侧边栏展示会话树的目录分组（默认全部展开）：

![首次启动](/screenshots/desktop-first-launch.webp)

在输入框左上角点击「选择工作目录…」，从最近打开列表中挑选，或点击「浏览…」打开系统目录选择器：

![工作目录下拉](/screenshots/desktop-workdir-dropdown.webp)

- 最近打开列表最多保留 10 个目录，按最近使用排序；hover 条目可点 × 移除
- 选择目录后输入区立即解锁，该目录置顶到最近列表
- 工作目录选择器只在新会话（还没有消息）时显示；会话开始后如需换目录，点击侧边栏「新对话」或直接在会话树中切换

## SSH 远程主机

不满足于本地代码时，可以在工作目录选择器左侧把会话切到任意 SSH 主机上运行——整个 CLI 子进程经 `ssh <host> -- wave --stdio` 在远端执行，agent 的读写、搜索、bash、git 等全部工具天然作用在远端文件系统，体验与 VS Code Remote-SSH 一致：

![主机选择器](/screenshots/desktop-ssh-host-selector.webp)

- 主机选择器位于工作目录选择器左侧，仅在新会话（还没有消息）时显示；首项「本地」，其后列出 `~/.ssh/config` 中解析出的主机（顶层 `Host` 块名称，通配符条目除外），末项「添加主机…」
- 选择主机后，工作目录选择器切换为该主机的远程目录上下文：列出该主机的最近打开远程目录，并可通过「浏览…」打开 VS Code「打开文件夹」式目录浏览面板——从弹层底部输入关键词可实时筛选当前单级子目录并高亮匹配项（过滤后自动选中第一项，按 `↑`/`↓` 移动选中、`Enter` 进入选中目录，面包屑可点击跳转层级）；也可直接在筛选框输入 `/` 或 `~` 开头的完整路径后按 `Enter` 直达（`~` 自动展开为远端家目录，确认时校验存在性）

![远程目录浏览面板](/screenshots/desktop-remote-dir-browser.webp)

- 「添加主机…」展开内联输入框，接受 `ssh user@hostname -p port` 形式的连接串，确认后写入 `~/.ssh/config`（追加 Host 块）并自动选中该主机；主机名已存在时提示且不覆盖
- 连接不要求交互式认证：无密钥/无 ssh-agent 时快速失败并显示可操作的错误提示
- 远端首次连接时自动探测 Node.js（需 ≥ 22）与 `wave` 命令，缺失时经 npmmirror 自动安装 `wave-code`，失败则显示含手动安装命令的错误信息；已装版本低于应用要求时自动升级到 `wave-code@<应用版本>`，升级成功后重启该主机的 daemon（历史会话可继续使用，进行中的任务终止）
- 每次启动主机选择重置为「本地」（与每次启动全新开始的规则一致）；各主机的最近打开目录在重启后仍可用

远程会话与本地会话共用同一套对话/权限确认/任务列表交互，侧边栏会话树中远程会话单独分组（分组键含主机），互不串话。

### 远程会话的面板

远程会话中，终端、差异与预览三类面板全部可用：

- **终端面板**：经本机 ssh 在远端分配 PTY 运行用户默认 shell，工作目录为分屏 agent 的当前工作目录（远端路径）；按键透传、ANSI 渲染、`Ctrl+C` 中断、尺寸调整与本地终端一致

![远程终端面板](/screenshots/desktop-ssh-remote-terminal.webp)

- **差异面板**：git 命令与文件读取全部经 ssh 在远端执行，展示远程仓库相对 HEAD 的工作区差异（含未跟踪文件），数据经 ssh 管道回传本机渲染；行评论与本地一致

![远程差异面板](/screenshots/desktop-ssh-remote-diff.webp)

- **预览面板**：与本地一致可用——点击远程会话消息中的 localhost 链接时，按需建立 SSH 端口转发（`ssh -N -L`，仅绑定本机回环 127.0.0.1；本地端口默认与远端端口相同，被占用时递增至首个空闲端口），将链接重写为转发后的本地地址并在预览面板中加载，而不是跳转系统默认浏览器；同一（主机，端口）的转发复用，重复点击不重复建立；转发建立失败或远端服务不可达时，预览面板显示可操作的错误提示与重试入口（与本地预览「无法连接」一致）；元素评论记录远端原始地址，确保 agent 在远端可访问该 URL

## 侧边栏会话树

侧边栏以二级树展示会话历史，无需打开弹窗即可跨目录切换会话：

- **一级**：目录分组，可折叠/展开；启动时默认全部展开
- **二级**：每个目录下的全部会话（无数量上限），按创建时间倒序排列——最新创建的在最上，会话后续的活跃（发消息、生成结束、被选中）不改变其位置；只有新建或删除会话时才重新排序，分组整体跟随其下最新创建的会话定位
- **运行中标识**：正在生成回复的会话前显示绿色圆点，可同时存在多个
- **待确认标识**：存在等待用户响应的确认请求（工具权限、计划确认、问答等）的会话前显示橙色圆点；待确认与运行中同时成立时优先显示待确认

![会话树](/screenshots/desktop-session-tree.webp)

点击分组标题即可折叠/展开：

![会话树折叠](/screenshots/desktop-session-tree-collapsed.webp)

点击会话的行为：

- 当前目录下的会话 → 直接在当前窗口恢复
- 其他目录下的会话 → 自动切换工作目录后恢复该会话
- 切换会话或目录不中止任何正在后台生成的会话

悬停会话条目可点击删除图标删除会话，删除前会弹出应用内确认对话框；worktree 会话会额外提示将一并删除 worktree 目录与临时分支：

![删除会话确认对话框](/screenshots/desktop-confirm-delete.webp)

### 侧边栏收起与展开

需要全宽看对话时，点击侧边栏头部右侧的收起按钮即可把侧边栏整体隐藏，聊天区占满窗口宽度；此时最左侧对话头部左上角出现展开按钮，点击即恢复侧边栏。收起状态跨分屏全局生效（分屏时展开按钮也只在最左侧对话头部显示），并持久保存——重启应用后保持上次的收起/展开选择：

![侧边栏收起](/screenshots/desktop-sidebar-collapsed.webp)

## 会话状态看板

侧边栏头部左侧的「活动」按钮打开会话状态看板——把所有目录的会话按当前状态分三列展示（等待中 / 运行中 / 已完成），一眼看清哪些会话在等你确认、哪些还在后台生成：

![会话状态看板](/screenshots/desktop-session-board.webp)

- 顶部「返回当前会话」退出看板；「项目筛选」下拉按工作目录过滤会话
- 三列归类：等待确认的会话（黄色圆点）最优先，运行中（灰蓝）次之，其余归入已完成（绿色）；等待确认与运行中同时成立时归入等待中
- 点任意会话卡片即恢复对应会话（跨目录行为与会话树一致）；看板为只读总览，切换会话不中止后台生成

## 基于分支的 worktree 隔离会话 {#worktree-isolation}

当你选择的工作目录是 git 仓库、且处于新会话状态（消息列表为空）时，工作目录选择器旁会显示分支选择器与「worktree」勾选项，让你在基于所选分支创建的临时 worktree 中开会话，并行试验而不污染主工作区：

![worktree 隔离会话](/screenshots/desktop-worktree-controls.webp)

- 分支选择器默认选中当前分支；勾选 worktree（默认勾选）后开始会话，应用会基于所选分支创建随机命名的临时分支与 worktree，会话实际工作目录为该 worktree 路径
- 取消勾选则在所选目录中直接开会话
- 工作目录不是 git 仓库或 git 不可用时不显示这两项控件
- 若项目配置了 WorktreeCreate 钩子，worktree 创建由钩子接管（hook 自行执行 `git worktree add` 并在 stdout 输出 worktree 路径，与 `wave -w` 一致）

## 会话切换快捷键

多会话并行后，可用键盘在会话间循环切换，无需鼠标点侧边栏：

- `Ctrl+Tab` / `Ctrl+Shift+Tab`：按侧边栏会话树的展平顺序正向 / 反向循环激活会话（跨目录分组，末尾回卷到首条）
- macOS 上另可用 `Cmd+Shift+]` / `Cmd+Shift+[` 等效切换
- 应用菜单栏有「下一个对话」「上一个对话」菜单项并显示快捷键，点击与按键等效
- 切换到其他目录的会话时行为与鼠标点击一致：切换工作目录、不中止后台生成中的会话；折叠分组中的会话照常参与循环

## 权限模式快捷键

按 `Cmd+Shift+M`（macOS）/ `Ctrl+Shift+M`（Windows/Linux）直接打开权限模式菜单（与 Claude Code Desktop 一致），也可点击输入框左下角的模式按钮展开；「对话」菜单下的「权限模式…」菜单项等效并显示快捷键。权限模式说明见[权限模式管理](/vsce#permission-modes)。

## 并排多对话（分屏）

`Cmd/Ctrl+Click` 侧边栏里的会话（或把会话条目拖入聊天区），即可在右侧新增一个分屏并排展示该会话——多条会话并排陈列、各自独立输入与流式生成，把多会话并行从后台能力升级为可见的多任务工作台：

![分屏多对话](/screenshots/desktop-split-view.webp)

- 普通点击（无修饰键）替换焦点分屏；`Cmd/Ctrl+Click` 新增分屏；目标会话已在某分屏展示时不重复新增，改为聚焦该分屏
- `Cmd/Ctrl+Shift+N`（macOS 为 `Cmd+Shift+N`）一步并排打开空白新会话；`Cmd+W` 关闭当前分屏（仅剩一个时不提供关闭入口）
- 各分屏的消息流、流式状态、任务列表与确认交互互不影响，多个分屏可同时流式生成；关闭正在生成的分屏后会话在后台继续运行（侧边栏运行中标识保留）
- 拖拽分屏头部可重排左右顺序，拖拽分屏间的分隔条可调整宽度（最小 360px）；手动调过的宽度比例在新增/关闭分屏时按比例保持

横向空间拥挤时，把分屏拖到聊天区上/下边缘可形成上下两行分屏（最多两行），利用垂直空间对照更多会话；拖拽行间分隔条调整行高（最小 280px），把下行分屏拖回上行即消除空行：

![上下两行分屏](/screenshots/desktop-two-row-split.webp)

## 对话级面板开关

每个对话分屏头部右上角有「面板」开关，可同时勾选预览、差异、终端三类面板在该对话内部并排展示，按任务需要组合每个对话的工作区视图：

![面板开关菜单](/screenshots/desktop-panel-toggle.webp)

- 点击开关展开多选菜单，各项显示勾选状态与快捷键；点击切换勾选，菜单不自动关闭（支持连续多选），点击外部或 Esc 关闭
- 快捷键：预览 `⇧⌘P`、差异 `⇧⌘D`、终端 `⌃\``（Windows 为 `Ctrl+Shift+P`/`Ctrl+Shift+D`/`Ctrl+\``）
- 面板是**对话级别**的——位于所属分屏内部、随分屏创建/切换/关闭，窗口级不存在跨分屏共享的面板实例；多个勾选项按预览→差异→终端的固定顺序从左到右并排
- 取消勾选即隐藏，再次勾选状态保留（预览不重新加载、终端会话与输出不丢失、差异重新拉取）
- 横向空间不足时面板可拖到消息区下方形成第二行（最多两行）；面板宽度可独立拖拽调整（最小 320px）
- **空间不足自动替换**：勾选新面板或点击文件路径/localhost 链接触发面板开启时，若空间不足，按勾选先后自动关闭旧面板（最早勾选的先关）腾出空间后开启，并给出轻量提示；关闭全部旧面板后仍不足（消息区将低于 360px）才拒绝开启，且不连带关闭任何旧面板。被替换的面板内容保留（预览不重载、终端会话不丢失），重新勾选即可恢复显示

### 差异面板

勾选「差异」后，面板以手风琴形式逐文件展示该对话工作目录（worktree 会话为 worktree 路径）的未提交 git 改动——含 agent 刚做的修改：

![差异面板](/screenshots/desktop-diff-pane.webp)

- 每个文件一个可折叠区块（默认展开，点击标题折叠/展开），头部显示相对路径、状态标识（新增/修改/删除/未跟踪等）与增删行统计，展开区显示差异内容（增删行高亮）
- **词级高亮**：改动行内逐词对比并高亮具体变化的最小片段（基于 `diffWords`），而非整行标红/标绿；纯增/纯删块仍整行高亮，配对在上下文行与 hunk 边界处重置，跨 hunk 不泄漏
- 一轮生成结束后自动刷新为最新改动，也提供手动刷新按钮；刷新保留旧内容直至新数据到达，不闪烁
- 非 git 仓库显示「非 git 仓库」空状态；无改动显示「无改动」空状态

悬停任一差异行，行右侧浮现「+」评论按钮（GitHub / GitLab 风格），点击在该行下方展开内联评论框：

![差异行评论](/screenshots/desktop-diff-comment.webp)

评论框显示所属文件路径，回车或点击右下角「添加」把这条评论连同文件路径、行前缀与行内容追加到下方的输入框（Esc 或「取消」关闭）；评论不会立刻发送给 agent，方便你连续标注多处改动、逐条积累后统一发送：

![差异评论追加到输入框](/screenshots/desktop-diff-comment-input.webp)

刷新或切换会话时改动行可能被覆盖，评论框与草稿自动清空。

### 文件面板

点击消息中 read / write / edit 工具调用的文件路径，会在该对话内打开一个只读文件查看器（同样在右键面板区域，宽度可拖拽调整），无需离开对话即可核对 agent 读写的文件内容：

![文件面板 - 代码](/screenshots/desktop-file-panel-code.webp)

- **本地与远程一致**：本地会话直接从磁盘读取；远程会话经 ssh 读取远端文件（仅返回文件内容，不落盘本机）
- **顶部文件搜索**：面板顶部提供搜索框，输入文件名片段即可查找并打开工作区文件（无需在消息流中翻找路径）；复用 @ 文件搜索的数据流，搜索范围锚定会话工作目录，本地与远程通用。聚焦即展示工作区顶部文件，输入停止约 200ms 后请求匹配结果，下拉朝下展开；目录项不展示，`↑`/`↓` + `Enter` 或点击选中后打开文件并清空搜索；过期请求按 requestId 去重，各分屏搜索状态相互独立
- **图片内联预览**：本地与远程的图片文件（png / jpg / gif / webp / svg / bmp / ico）都在面板内直接渲染预览，不再打开系统默认应用；远程图片也能正常预览

![文件面板 - 本地图片预览](/screenshots/desktop-file-panel-image.webp)

- **Markdown 与代码渲染**：Markdown 文件按渲染后的排版展示；代码文件按扩展名语法高亮并显示行号
- **跳转到读取区间**：read 工具带 `offset`/`limit` 时，路径显示 `:起始:数量` 后缀，点击后面板滚动到对应行并高亮该区间（与工具实际读取的行一致）
- **大文件截断**：超过行数/大小上限的文件仅展示前部内容，面板顶部提示共多少行、仅显示前多少行
- **工具栏**：左侧标注文件归属（本地 / 所属 SSH 主机）与相对路径（悬停显示完整路径），过长时中间省略以保留文件名；右侧提供关闭；本地会话额外提供「在默认应用中打开」

![文件面板 - 远程图片预览](/screenshots/desktop-file-panel-image-remote.webp)

- 再次点击已打开的文件会重新读取（保留旧内容直至新内容到达，不闪烁）；切换会话后面板随该对话关闭

### 终端面板

勾选「终端」后，面板内嵌一个真正的终端（基于 node-pty），以该对话工作目录为 cwd 启动用户默认 shell，可运行 `vim`、`top` 等交互式程序：

![终端面板](/screenshots/desktop-terminal-pane.webp)

- 按键全量透传、ANSI 颜色正确渲染、`Ctrl+C` 中断前台程序，行为与系统终端一致
- 调整面板宽度时同步终端行列数，输出按新宽度重排
- 取消勾选再重新勾选，shell 会话与滚动输出原样保留（隐藏不终止）；工具栏提供「重启终端」操作
- 关闭分屏、删除会话或退出应用时终止其终端进程，不残留孤儿进程

预览面板即下文「localhost 原型预览」的预览面板，由「只能点击链接打开」收编为可显式开关的面板之一；点击消息中的 localhost 链接会自动勾选预览项并加载该 URL。

## localhost 原型预览与元素评论

让 agent 启动本地开发服务器（如 Vite 原型）后，点击消息中的 localhost 链接（`localhost`、`127.0.0.1`、`[::1]`，任意端口）会在窗口右侧打开预览面板，而不是跳转外部浏览器 —— 边对话边看原型：

![原型预览面板](/screenshots/desktop-preview-pane.webp)

- 非 localhost 的 http(s) 链接仍用系统默认浏览器打开
- 面板工具栏提供：地址栏、元素拾取开关、刷新、「在浏览器打开」、全屏预览、关闭；面板宽度可拖拽调整
- **多标签页**：面板顶部是标签栏——点击不同的 localhost 链接会各自打开一个标签页（同一 URL 复用已有标签页并选中），点标签切换、点标签上的 × 关闭（关闭当前选中的自动落到左侧邻居，关掉最后一个标签页即收起面板），末尾的 + 可新建空白标签页；空白/选中标签页可直接在地址栏输入网址（如 `localhost:4000`，自动补全 `http://`）导航，标签页内自身跳转时地址栏跟随更新：

![预览多标签页](/screenshots/desktop-preview-tabs.webp)

- **全屏预览**：点击工具栏「全屏预览」按钮（或 Esc 退出），预览面板占满整个内容区、对话区隐藏，适合专注查看原型效果；再次点击或按 Esc 恢复原布局（对话区与并排面板按全屏前的勾选与宽度还原）:

![预览全屏](/screenshots/desktop-preview-fullscreen.webp)

点击工具栏左侧的「元素拾取」开关进入拾取模式：鼠标悬停的元素显示高亮轮廓，页面自身的点击、跳转、表单提交会被拦截：

![元素拾取](/screenshots/desktop-preview-picker.webp)

点击目标元素后，旁边弹出评论卡片，写下要改什么，回车或点击右下角的添加图标把这条评论连同元素上下文追加到下方的输入框；点击卡片外空白处可取消并重新选择：

![元素评论](/screenshots/desktop-preview-comment.webp)

评论不会立刻发送给 agent，而是追加到消息列表下方的输入框，方便你连续拾取多个元素、逐条积累评论后统一编辑、一起发送。拾取模式保持开启，可以继续点选下一个元素；提交内容包含页面 URL、元素 CSS 选择器、元素摘要与你的评论文本，agent 据此精确理解「指的是哪里」并修改原型。再次点击拾取开关退出拾取；页面导航或刷新后拾取自动重置为关闭。

![评论追加到输入框](/screenshots/desktop-preview-comment-input.webp)

## 设置

账户卡片「更多」菜单里的「设置」打开桌面端全页面设置：左侧导航分组（全局设置 / 个性化 / 项目设置 / AI 与扩展……），右侧为对应分组的内容，改动后点击右下角「保存」生效：

![设置 - 全局设置](/screenshots/desktop-settings-global.webp)

「个性化」分组提供 AGENTS.md 与自动记忆规则的配置入口：用户级 / 项目级 AGENTS.md 的编辑与保存，以及自动记忆开关与记忆提取触发轮次：

![设置 - 个性化](/screenshots/desktop-settings-personalization.webp)

「AI 与扩展」分组提供技能 / 子代理 / 钩子 / MCP 服务四个管理视图，均按来源以 Tab 展示（插件 / 内置 / 用户 / 项目），插件与内置为只读来源，用户与项目级可管理，项目级内容按所属项目分组卡片展示：

- **技能**：点击「新建技能」或项目卡片「新增指令」，关闭设置页回到对话，AI 对话框预填提示词（如 `/settings 帮我新建一个用户级技能：<技能名>，用于<场景>`），补充描述发送后由 AI 创建 `SKILL.md`；「编辑」同时预填提示词并用系统编辑器打开 `SKILL.md`；「删除」二次确认后直接删除技能目录：

![设置 - 技能](/screenshots/desktop-settings-skills.webp)

- **子代理**：与技能同形态——新建预填提示词由 AI 创建 `AGENTS.md`，编辑打开 `AGENTS.md`，删除二次确认后直接删除定义文件：

![设置 - 子代理](/screenshots/desktop-settings-subagents.webp)

- **钩子**：按来源 Tab 展示钩子（如 `PreToolUse:Write`），每条带启用开关（创建后默认开启）、编辑与删除；「新增钩子」预填提示词，编辑打开对应 `settings.json`：

![设置 - 钩子](/screenshots/desktop-settings-hooks.webp)

- **MCP 服务**：按来源 Tab 展示 MCP 服务器（用户级存于 `~/.wave/mcp.json`，项目级存于项目根目录 `.mcp.json`，插件只读），支持连接 / 断开、新建（预填提示词）、编辑（打开配置文件）与删除（二次确认）；`/mcp` 斜杠命令直接唤起本视图：

![设置 - MCP 服务](/screenshots/desktop-settings-mcp.webp)

> 各视图的交互细节与 IDE 插件一致，详见 [VS Code / JetBrains 文档 - 第 6 章 多 Agents 与并发](/vsce#multi-agent-concurrency) 与 [第 7.4 节 钩子](/vsce#hooks-settings)。

## 核心交互

对话体验与 IDE 插件一致 —— 思考过程、工具调用、权限确认、斜杠命令、消息队列、图片粘贴等全部可用：

![核心交互](/screenshots/desktop-chat.webp)

把文件（支持多选）从资源管理器拖进对话窗口时，整个对话区浮现上传遮罩，松手即上传并作为附件挂到当前对话的输入框，无需经「+」菜单选择文件：

![文件拖拽上传](/screenshots/desktop-drag-upload.webp)

消息列表与输入框共用同一套最大宽度（800px）并居中显示，窗口越宽两侧留白越明显，对话内容始终与输入框左右对齐，避免消息拉满整行显得松散：

![居中的对话列](/screenshots/desktop-chat-centered.webp)

对话开始后，输入框工具栏右下角显示上下文用量指示器：圆环进度图标 + 百分比数字（环的填充比例与已用百分比一致，说明「已使用 XX%」在提示中），用于了解当前上下文占用情况；该指示器仅作展示不可点击，主动压缩请使用 `/compact` 斜杠命令或等待自动压缩：

![上下文用量指示器](/screenshots/desktop-compress-button.webp)

详细说明请移步：

- [基础对话与 AI 思考过程](/vsce#basic-chat)
- [斜杠命令与文件建议](/vsce#slash-commands)
- [权限模式管理](/vsce#permission-modes)
- [配置设置](/vsce#configuration-settings)

## 内置 CLI

应用内置 Wave CLI，本地会话由应用自带的 Electron 运行时直接驱动，**无需安装 Node.js / npm**。首次使用需要 grep 时，会按需下载 ripgrep（rg）二进制并缓存到 `~/.wave/cli/node_modules/`（与 IDE 插件共享缓存，不重复下载）；下载失败时初始化报错，检查网络后重试。

内置 CLI 的版本与应用版本一致（随应用发布）。开发场景可用 `WAVE_CLI_PATH` 环境变量指向工作区构建覆盖内置 CLI。

## 自动更新

应用在后台自动检查新版本（已登录企业版时经 codechat 下载端点，未登录回退 GitHub Releases），发现新版本时在右下角弹出应用内 toast 提示（非模态，不打断当前输入），无需理会、会自动消失：

![发现新版本 toast](/screenshots/desktop-update-toast.webp)

新版本下载完成后，toast 带「重启安装」按钮，点击即退出并安装新版本：

![更新就绪 toast](/screenshots/desktop-update-toast-action.webp)

- 下载完成后不弹二次确认对话框——toast 按钮即唯一操作入口；不需要时可点右上角 × 关闭，稍后账户卡片「更多」→ 设置中的「检查更新」可再次触发
- 更新服务故障（端点不可达 / 下载失败）或 macOS 上应用位于不可写位置时，toast 会带「打开下载页」按钮，引导手动下载安装，不静默失败

## 登录

桌面版支持 SSO 登录，复用与 IDE 插件相同的登录按钮与登录对话框；授权页面会在系统默认浏览器中打开，登录流程由内置 CLI 子进程完成。登录为可选项 —— 未登录时也可通过自定义配置（API Key / Base URL）直连模型服务。登录/登出入口位于账户卡片「更多」菜单（见下文）。

### 账户卡片

登录后，侧边栏底部显示当前账户卡片：头像与名称，右侧「更多」按钮提供设置、企业控制台、帮助文档、退出登录等操作（与 IDE 插件的「更多」菜单一致）。点击卡片本身展开用量弹窗，查看套餐用量（进度条 + 已用百分比）与 API 额度（团队共用余额时显示已消耗金额，个人额度显示剩余金额）：

![账户卡片与用量弹窗](/screenshots/desktop-account-card.webp)
