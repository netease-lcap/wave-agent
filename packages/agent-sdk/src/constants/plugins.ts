/**
 * Plugin system constants.
 */

/**
 * Version assumed when a plugin manifest omits `version` (spec plugin A-022).
 * Install and load must agree on it: a manifest that installs must also load.
 */
export const DEFAULT_PLUGIN_VERSION = "1.0.0";
