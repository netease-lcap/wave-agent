{
  "name": "wave-vsce",
  "private": true,
  "displayName": "Wave 代码智聊",
  "description": "Wave code for VS Code",
  "version": "0.19.2",
  "publisher": "wave-code",
  "icon": "LOGO.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/netease-lcap/wave-vsce.git"
  },
  "type": "module",
  "engines": {
    "vscode": "^1.107.0",
    "node": ">=20"
  },
  "files": [
    "dist/",
    "webview/dist/",
    "LOGO.png",
    "logo.svg",
    "LICENSE"
  ],
  "categories": [
    "Other"
  ],
  "main": "./dist/extension.cjs",
  "contributes": {
    "views": {
      "waveChatView": [
        {
          "id": "waveChat",
          "name": "代码智聊",
          "type": "webview"
        }
      ]
    },
    "viewsContainers": {
      "activitybar": [
        {
          "id": "waveChatView",
          "title": "Wave",
          "icon": "logo.svg"
        }
      ]
    },
    "commands": [
      {
        "command": "wave-code.openChatSidebar",
        "title": "在侧边栏打开代码智聊",
        "category": "Wave"
      },
      {
        "command": "wave-code.openChatTab",
        "title": "在新标签页打开代码智聊",
        "category": "Wave"
      },
      {
        "command": "wave-code.openChatWindow",
        "title": "在新窗口打开代码智聊",
        "category": "Wave"
      },
      {
        "command": "wave-code.focusView",
        "title": "聚焦到聊天输入框",
        "category": "Wave"
      },
      {
        "command": "wave-code.addToWave",
        "title": "添加到 Wave",
        "category": "Wave"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "command": "wave-code.addToWave",
          "when": "editorHasSelection",
          "group": "navigation"
        }
      ]
    }
  },
  "scripts": {
    "build": "pnpm run compile",
    "lint": "eslint --cache",
    "clean": "node -e \"import('fs').then(fs => ['dist', 'webview/dist'].forEach(d => {try{fs.rmSync(d, {recursive:true, force:true})}catch(e){}}))\"",
    "prepackage": "pnpm run clean",
    "compile": "pnpm run compile:backend && pnpm run sync:webview",
    "watch": "pnpm run watch:backend",
    "compile:backend": "node esbuild.config.mjs",
    "watch:backend": "node esbuild.config.mjs --watch",
    "sync:webview": "node scripts/syncWebview.mjs",
    "esbuild:prod": "node esbuild.config.mjs --production && node scripts/syncWebview.mjs",
    "package": "node scripts/package.mjs",
    "type-check": "tsc --noEmit && tsc -p tests --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "preinstall:vsix": "pnpm run package",
    "install:vsix": "code --install-extension releases/*.vsix",
    "push:tag": "git push origin $(git describe --tags --abbrev=0)"
  },
  "devDependencies": {
    "@types/node": "^25.0.1",
    "@types/vscode": "^1.95.0",
    "@vscode/vsce": "^3.9.1",
    "esbuild": "^0.28.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.5",
    "wave-agent-sdk": "workspace:*",
    "wave-webview": "workspace:*"
  }
}
