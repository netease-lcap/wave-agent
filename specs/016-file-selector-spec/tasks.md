# File Selector Tasks

- [x] Research existing `FileSelector` and `InputManager` logic
- [x] Document triggering and activation mechanism
- [x] Document search and debouncing logic
- [x] Define data models and interfaces
- [ ] Add unit tests for `InputManager` file selector state transitions
- [ ] Add unit tests for `FileSelector` component navigation
- [ ] Implement fuzzy search for file matching
- [ ] Add support for home directory (`~`) expansion
