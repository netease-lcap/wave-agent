# Tasks: ACP Implementation

## Completed Tasks
- [x] Research ACP protocol and Wave implementation.
- [x] Analyze `prompt` function in `WaveAcpAgent`.
- [x] Analyze mode transitions for `ExitPlanMode` tool.
- [x] Fix `allow_always` for `ExitPlanMode` to switch to `acceptEdits` mode.
- [x] Create spec files in `specs/022-acp-protocol`.
- [x] Add test case for `ExitPlanMode` with `allow_always` in `packages/code/tests/acp/agent.test.ts`.

## Future Tasks
- [ ] Add more comprehensive tests for ACP mode transitions.
- [ ] Support streaming tool arguments in ACP.
- [ ] Improve error handling for malformed ACP requests.
